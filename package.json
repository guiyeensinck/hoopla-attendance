{
  "name": "hoopla-attendance",
  "version": "1.0.0",
  "description": "Slack attendance tracker for Hoopla — entry, lunch, exit logging with dashboard and reports",
  "main": "src/app.js",
  "scripts": {
    "start": "node src/app.js",
    "dev": "nodemon src/app.js",
    "init-db": "node src/database.js --init",
    "test": "echo \"No tests yet\" && exit 0"
  },
  "dependencies": {
    "@slack/bolt": "^3.17.0",
    "better-sqlite3": "^11.0.0",
    "dayjs": "^1.11.10",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "exceljs": "^4.4.0",
    "node-cron": "^3.0.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
